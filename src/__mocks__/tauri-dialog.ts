/** Stub for @tauri-apps/api/dialog — unavailable in the test environment. */
export const open = async (): Promise<null> => null;
export const save = async (): Promise<null> => null;
export const message = async (): Promise<void> => {};
export const ask = async (): Promise<boolean> => false;
export const confirm = async (): Promise<boolean> => false;
