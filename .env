# Disable WebKit2GTK DMA-BUF renderer on Linux (Ubuntu 22.04).
# Without this, the WebView fails to allocate GBM/DRM buffers on machines
# that lack direct GPU device access, resulting in a black screen.
WEBKIT_DISABLE_DMABUF_RENDERER=1
